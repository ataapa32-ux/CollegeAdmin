<Page x:Class="CollegeAdmin.Pages.StudentsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Студенты">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView x:Name="StudentsListView" Grid.Row="0" ItemsSource="{Binding Students}" SelectedItem="{Binding SelectedStudent}" Margin="0,5,0,5">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Фамилия" DisplayMemberBinding="{Binding LastName}" Width="100"/>
                    <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding FirstName}" Width="100"/>
                    <GridViewColumn Header="Отчество" DisplayMemberBinding="{Binding MiddleName}" Width="100"/>
                    <GridViewColumn Header="Группа" DisplayMemberBinding="{Binding Group}" Width="80"/>
                    <GridViewColumn Header="Курс" DisplayMemberBinding="{Binding Course}" Width="50"/>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Редактировать профиль" Width="150" Margin="0,0,10,0" Click="EditProfileButton_Click"/>
        </StackPanel>
    </Grid>
</Page>





